export default Ember.Route.extend({
  //setupController: function(controller, collection) {
    //console.log(collection.get('photos'));
    //controller.set('photos', collection.get('photos'));
  //}
});

